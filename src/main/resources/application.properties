#profile
spring.config.activate.on-profile=stage

#RDB
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://inhouse.cpx3y0rpnqaj.ap-northeast-2.rds.amazonaws.com:3306/back_office
spring.datasource.username=admin
spring.datasource.password=VMFOR*()!#
spring.sql.init.mode=never

#logging
logging.level.root=info
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.type=info

#port
server.port=8080

#jpa
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

#encryption
METADATA_ENCRYPT_KEY=${ENCRYPT_KEY}

#slack
slack.token=${SLACK_TOKEN}
slack.webbhook-url=${SLACK_WEBHOOK_URL}
slack.app-signing-secret=${SLACK_APP_SIGNING_SECRET}
